## 📁 Command: setting

### /set

**Deskripsi:** Konfigurasi pengaturan Bot

### Usage:
`/set automod whitelist action [action]`
`/set automod whitelist target [target]`
`/set automod badwords action [action]`
`/set automod badwords word [word]`
`/set automod exception-channel action [action]`
`/set automod exception-channel channel [channel]`
`/set automod log-channel channel [channel]`
`/set fitur anti-invites status [status]`
`/set fitur anti-links status [status]`
`/set fitur anti-spam status [status]`
`/set fitur anti-badwords status [status]`
`/set fitur server-stats status [status]`
`/set fitur economy status [status]`
`/set fitur giveaway status [status]`
`/set fitur invites status [status]`
`/set fitur suggestion status [status]`
`/set fitur ticket status [status]`
`/set fitur pet status [status]`
`/set fitur clan status [status]`
`/set fitur adventure status [status]`
`/set fitur leveling status [status]`
`/set fitur welcome-in status [status]`
`/set fitur welcome-out status [status]`
`/set fitur nsfw status [status]`
`/set fitur checklist status [status]`
`/set fitur minecraft-stats status [status]`
`/set stats member-count-channel channel [channel]`
`/set stats online-count-channel channel [channel]`
`/set admin edit action [action]`
`/set admin edit target [target]`
`/set welcome in-channel channel [channel]`
`/set welcome out-channel channel [channel]`
`/set welcome role role [role]`
`/set welcome in-text text [text]`
`/set welcome out-text text [text]`
`/set cooldown daily cooldown [cooldown]`
`/set cooldown beg cooldown [cooldown]`
`/set cooldown lootbox cooldown [cooldown]`
`/set cooldown work cooldown [cooldown]`
`/set cooldown rob cooldown [cooldown]`
`/set cooldown hack cooldown [cooldown]`
`/set cooldown pet cooldown [cooldown]`
`/set cooldown gacha cooldown [cooldown]`
`/set leveling channel channel [channel]`
`/set leveling cooldown cooldown [cooldown]`
`/set leveling xp xp [xp]`
`/set leveling rolereward action [action]`
`/set leveling rolereward level [level]`
`/set leveling rolereward role [role]`
`/set minecraft setup category [category]`
`/set minecraft ip ip [ip]`
`/set minecraft port port [port]`
`/set minecraft ip-channel channel [channel]`
`/set minecraft port-channel channel [channel]`
`/set minecraft status-channel channel [channel]`
`/set language lang [lang]`

### Options:
### Untuk perintah (automod > whitelist > action):
- `action` (required) — Tambah/hapus
  - Pilihan: `add`, `remove`
  - **Contoh Penggunaan:** `/set automod whitelist action action:add`
### Untuk perintah (automod > whitelist > target):
- `target` (required) — User/role
  - **Contoh Penggunaan:** `/set automod whitelist target target:[value]`
### Untuk perintah (automod > badwords > action):
- `action` (required) — Tambah/hapus
  - Pilihan: `add`, `remove`
  - **Contoh Penggunaan:** `/set automod badwords action action:add`
### Untuk perintah (automod > badwords > word):
- `word` (required) — Kata
  - **Contoh Penggunaan:** `/set automod badwords word word:[value]`
### Untuk perintah (automod > exception-channel > action):
- `action` (required) — Tambah/hapus
  - Pilihan: `add`, `remove`
  - **Contoh Penggunaan:** `/set automod exception-channel action action:add`
### Untuk perintah (automod > exception-channel > channel):
- `channel` (required) — Channel untuk pengecualian
  - **Contoh Penggunaan:** `/set automod exception-channel channel channel:[value]`
### Untuk perintah (automod > log-channel > channel):
- `channel` (required) — Pilih channel untuk dijadikan log automod
  - **Contoh Penggunaan:** `/set automod log-channel channel channel:[value]`
### Untuk perintah (fitur > anti-invites > status):
- `status` (required) — Aktifkan/nonaktifkan
  - Pilihan: `enable`, `disable`
  - **Contoh Penggunaan:** `/set fitur anti-invites status status:enable`
### Untuk perintah (fitur > anti-links > status):
- `status` (required) — Aktifkan/nonaktifkan
  - Pilihan: `enable`, `disable`
  - **Contoh Penggunaan:** `/set fitur anti-links status status:enable`
### Untuk perintah (fitur > anti-spam > status):
- `status` (required) — Aktifkan/nonaktifkan
  - Pilihan: `enable`, `disable`
  - **Contoh Penggunaan:** `/set fitur anti-spam status status:enable`
### Untuk perintah (fitur > anti-badwords > status):
- `status` (required) — Aktifkan/nonaktifkan
  - Pilihan: `enable`, `disable`
  - **Contoh Penggunaan:** `/set fitur anti-badwords status status:enable`
### Untuk perintah (fitur > server-stats > status):
- `status` (required) — Aktifkan/nonaktifkan
  - Pilihan: `enable`, `disable`
  - **Contoh Penggunaan:** `/set fitur server-stats status status:enable`
### Untuk perintah (fitur > economy > status):
- `status` (required) — Aktifkan/nonaktifkan
  - Pilihan: `enable`, `disable`
  - **Contoh Penggunaan:** `/set fitur economy status status:enable`
### Untuk perintah (fitur > giveaway > status):
- `status` (required) — Aktifkan/nonaktifkan
  - Pilihan: `enable`, `disable`
  - **Contoh Penggunaan:** `/set fitur giveaway status status:enable`
### Untuk perintah (fitur > invites > status):
- `status` (required) — Aktifkan/nonaktifkan
  - Pilihan: `enable`, `disable`
  - **Contoh Penggunaan:** `/set fitur invites status status:enable`
### Untuk perintah (fitur > suggestion > status):
- `status` (required) — Aktifkan/nonaktifkan
  - Pilihan: `enable`, `disable`
  - **Contoh Penggunaan:** `/set fitur suggestion status status:enable`
### Untuk perintah (fitur > ticket > status):
- `status` (required) — Aktifkan/nonaktifkan
  - Pilihan: `enable`, `disable`
  - **Contoh Penggunaan:** `/set fitur ticket status status:enable`
### Untuk perintah (fitur > pet > status):
- `status` (required) — Aktifkan/nonaktifkan
  - Pilihan: `enable`, `disable`
  - **Contoh Penggunaan:** `/set fitur pet status status:enable`
### Untuk perintah (fitur > clan > status):
- `status` (required) — Aktifkan/nonaktifkan
  - Pilihan: `enable`, `disable`
  - **Contoh Penggunaan:** `/set fitur clan status status:enable`
### Untuk perintah (fitur > adventure > status):
- `status` (required) — Aktifkan/nonaktifkan
  - Pilihan: `enable`, `disable`
  - **Contoh Penggunaan:** `/set fitur adventure status status:enable`
### Untuk perintah (fitur > leveling > status):
- `status` (required) — Aktifkan/nonaktifkan
  - Pilihan: `enable`, `disable`
  - **Contoh Penggunaan:** `/set fitur leveling status status:enable`
### Untuk perintah (fitur > welcome-in > status):
- `status` (required) — Aktifkan/nonaktifkan
  - Pilihan: `enable`, `disable`
  - **Contoh Penggunaan:** `/set fitur welcome-in status status:enable`
### Untuk perintah (fitur > welcome-out > status):
- `status` (required) — Aktifkan/nonaktifkan
  - Pilihan: `enable`, `disable`
  - **Contoh Penggunaan:** `/set fitur welcome-out status status:enable`
### Untuk perintah (fitur > nsfw > status):
- `status` (required) — Aktifkan/nonaktifkan
  - Pilihan: `enable`, `disable`
  - **Contoh Penggunaan:** `/set fitur nsfw status status:enable`
### Untuk perintah (fitur > checklist > status):
- `status` (required) — Aktifkan/nonaktifkan
  - Pilihan: `enable`, `disable`
  - **Contoh Penggunaan:** `/set fitur checklist status status:enable`
### Untuk perintah (fitur > minecraft-stats > status):
- `status` (required) — Aktifkan/nonaktifkan
  - Pilihan: `enable`, `disable`
  - **Contoh Penggunaan:** `/set fitur minecraft-stats status status:enable`
### Untuk perintah (stats > member-count-channel > channel):
- `channel` (required) — Channel untuk stats
  - **Contoh Penggunaan:** `/set stats member-count-channel channel channel:[value]`
### Untuk perintah (stats > online-count-channel > channel):
- `channel` (required) — Channel untuk stats
  - **Contoh Penggunaan:** `/set stats online-count-channel channel channel:[value]`
### Untuk perintah (admin > edit > action):
- `action` (required) — Tambah/hapus
  - Pilihan: `add`, `remove`
  - **Contoh Penggunaan:** `/set admin edit action action:add`
### Untuk perintah (admin > edit > target):
- `target` (required) — User/role admin
  - **Contoh Penggunaan:** `/set admin edit target target:[value]`
### Untuk perintah (welcome > in-channel > channel):
- `channel` (required) — Channel welcome in
  - **Contoh Penggunaan:** `/set welcome in-channel channel channel:[value]`
### Untuk perintah (welcome > out-channel > channel):
- `channel` (required) — Channel welcome out
  - **Contoh Penggunaan:** `/set welcome out-channel channel channel:[value]`
### Untuk perintah (welcome > role > role):
- `role` (required) — Role untuk welcome
  - **Contoh Penggunaan:** `/set welcome role role role:[value]`
### Untuk perintah (welcome > in-text > text):
- `text` (required) — Teks untuk welcome in
  - **Contoh Penggunaan:** `/set welcome in-text text text:[value]`
### Untuk perintah (welcome > out-text > text):
- `text` (required) — Teks untuk welcome out
  - **Contoh Penggunaan:** `/set welcome out-text text text:[value]`
### Untuk perintah (cooldown > daily > cooldown):
- `cooldown` (required) — Cooldown dalam detik
  - **Contoh Penggunaan:** `/set cooldown daily cooldown cooldown:[value]`
### Untuk perintah (cooldown > beg > cooldown):
- `cooldown` (required) — Cooldown dalam detik
  - **Contoh Penggunaan:** `/set cooldown beg cooldown cooldown:[value]`
### Untuk perintah (cooldown > lootbox > cooldown):
- `cooldown` (required) — Cooldown dalam detik
  - **Contoh Penggunaan:** `/set cooldown lootbox cooldown cooldown:[value]`
### Untuk perintah (cooldown > work > cooldown):
- `cooldown` (required) — Cooldown dalam detik
  - **Contoh Penggunaan:** `/set cooldown work cooldown cooldown:[value]`
### Untuk perintah (cooldown > rob > cooldown):
- `cooldown` (required) — Cooldown dalam detik
  - **Contoh Penggunaan:** `/set cooldown rob cooldown cooldown:[value]`
### Untuk perintah (cooldown > hack > cooldown):
- `cooldown` (required) — Cooldown dalam detik
  - **Contoh Penggunaan:** `/set cooldown hack cooldown cooldown:[value]`
### Untuk perintah (cooldown > pet > cooldown):
- `cooldown` (required) — Cooldown dalam detik
  - **Contoh Penggunaan:** `/set cooldown pet cooldown cooldown:[value]`
### Untuk perintah (cooldown > gacha > cooldown):
- `cooldown` (required) — Cooldown dalam detik
  - **Contoh Penggunaan:** `/set cooldown gacha cooldown cooldown:[value]`
### Untuk perintah (leveling > channel > channel):
- `channel` (required) — Channel untuk pesan level up
  - **Contoh Penggunaan:** `/set leveling channel channel channel:[value]`
### Untuk perintah (leveling > cooldown > cooldown):
- `cooldown` (required) — Cooldown dalam detik
  - **Contoh Penggunaan:** `/set leveling cooldown cooldown cooldown:[value]`
### Untuk perintah (leveling > xp > xp):
- `xp` (required) — Jumlah XP yang didapat per pesan
  - **Contoh Penggunaan:** `/set leveling xp xp xp:[value]`
### Untuk perintah (leveling > rolereward > action):
- `action` (required) — Tambah atau hapus role reward
  - Pilihan: `add`, `remove`
  - **Contoh Penggunaan:** `/set leveling rolereward action action:add`
### Untuk perintah (leveling > rolereward > level):
- `level` (required) — Level yang dibutuhkan
  - **Contoh Penggunaan:** `/set leveling rolereward level level:[value]`
### Untuk perintah (leveling > rolereward > role):
- `role` (required) — Role yang akan diberikan
  - **Contoh Penggunaan:** `/set leveling rolereward role role:[value]`
### Untuk perintah (minecraft > setup > category):
- `category` (required) — Kategori tempat channel dibuat
  - **Contoh Penggunaan:** `/set minecraft setup category category:[value]`
### Untuk perintah (minecraft > ip > ip):
- `ip` (required) — IP server Minecraft
  - **Contoh Penggunaan:** `/set minecraft ip ip ip:[value]`
### Untuk perintah (minecraft > port > port):
- `port` (required) — Port server Minecraft
  - **Contoh Penggunaan:** `/set minecraft port port port:[value]`
### Untuk perintah (minecraft > ip-channel > channel):
- `channel` (required) — Channel untuk IP Minecraft
  - **Contoh Penggunaan:** `/set minecraft ip-channel channel channel:[value]`
### Untuk perintah (minecraft > port-channel > channel):
- `channel` (required) — Channel untuk port Minecraft
  - **Contoh Penggunaan:** `/set minecraft port-channel channel channel:[value]`
### Untuk perintah (minecraft > status-channel > channel):
- `channel` (required) — Channel untuk status Minecraft
  - **Contoh Penggunaan:** `/set minecraft status-channel channel channel:[value]`
### Untuk perintah (language > lang):
- `lang` (required) — Choose language
  - Pilihan: `id`, `en`
  - **Contoh Penggunaan:** `/set language lang lang:id`

---

